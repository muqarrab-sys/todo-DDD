export declare interface IOAuthConfigs {
  clientId: string;
  projectId: string;
  authUri: string;
  tokenUri: string;
  auth_provider_x509_cert_url: string;
  clientSecret: string;
  redirectUri: string;
}
